import React from 'react';
import styles from "./HotelSearch.module.scss";

const HotelsBlock = () => {
    return (
        <div className={`${styles.hotels_block} ${styles.block}`}>
            hotels
        </div>
    );
};

export default HotelsBlock;